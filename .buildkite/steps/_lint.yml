label: ":golangci-lint: lint"
key: lint

# qdb-artifacts runs first (pre-command hook order) to populate qdb/ with
# CGO headers before the golangci-lint container starts.
plugins:
  - bureau14/qdb-artifacts#master:
      download:
        project_id: "quasardb"
        output-dir: "qdb"
        extract: true
        clean: true
        files:
          - "*-c-api.tar.zst!*"
          - "*-server.tar.zst!*"
          - "*-utils.tar.zst!*"
      promote: {}
  - golangci-lint#v1.0.0: {}

retry:
  automatic:
    limit: 3

timeout_in_minutes: 15

agents:
  queue: "default-debian-amd64"
