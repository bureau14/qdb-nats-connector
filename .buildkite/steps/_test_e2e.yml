label: ":construction: test-e2e ({slug}) [stub]"
key: test-e2e-{slug}
depends_on: test-integration-{slug}

commands:
  - bash scripts/cicd/50.test-e2e.sh

retry:
  automatic:
    limit: 3

timeout_in_minutes: 5

agents:
  queue: "default-{queue}"
